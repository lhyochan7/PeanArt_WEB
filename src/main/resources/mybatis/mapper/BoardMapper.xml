<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.peanart.Board.dao.BoardDAO">
    <select id="getBoardList" resultType="BoardVO">
        SELECT A.exhib_seq, A.exhib_title, A.exhib_simple_exp, A.exhib_start_date, A.exhib_location,
        B.file_dir_name, B.file_name
        FROM tb_exhibit as A LEFT JOIN tb_file_exhibit as B ON A.exhib_seq = B.exhib_seq
    </select>

</mapper>
